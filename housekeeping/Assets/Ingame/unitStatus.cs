using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class unitStatus : MonoBehaviour
{
    int attackPoint;
    int healthPoint;
    int costPoint;
    int movePoint;
    int locationPoint;
    int skillPoint;

    // Start is called before the first frame update
    void Start()
    {
        attackPoint = 0; healthPoint = 0; movePoint = 0; locationPoint = 0; skillPoint = 0;
    }

    // Update is called once per frame
    void Update()
    {
        //턴증가시 행동
    }
}
